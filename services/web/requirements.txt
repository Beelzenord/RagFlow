fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
httpx==0.27.2
# Signs the login session cookie (Starlette's SessionMiddleware).
itsdangerous==2.2.0
# The ElevenLabs TTS client socket. uvicorn[standard] happens to pull this in for
# its own server-side use, so voice would import fine today and break silently on
# a uvicorn bump - pinned here as a direct dependency instead. The
# websockets.asyncio.client entry point this uses needs >= 13.
websockets==14.2
